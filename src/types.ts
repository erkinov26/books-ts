export interface Book {
  id: string;
  title: string;
  description: string;
}

export interface dummyType {
  title: string,
  type: string,
  placeholder: string,
  name: string,
  maxLength: number,
}